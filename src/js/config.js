//some domain don't allow request coming from other domain i.e. your file is currently runnung on localhost so hhtp request is not allowed by "api" because authors of api didn't implement that funtionality in that case we have to use "CORS" proxy
//export const proxy = "https://cors-anywhere.herokuapp.com/";
export const key = '';